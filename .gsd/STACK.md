# Technology Stack

> Auto-generated by /map on 2026-02-23

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Python | ≥3.10 | Core runtime |
| Ollama | latest (external) | Local LLM inference host |
| LLM Model | nemotron-3-nano:latest | Language model via Ollama |

---

## Dependencies

### Production

| Package | Version Constraint | Purpose |
|---------|-------------------|---------|
| fastapi | ≥0.129.0 | REST API framework |
| uvicorn[standard] | ≥0.40.0 | ASGI server |
| python-multipart | ≥0.0.22 | File upload support |
| requests | ≥2.32.0 | Ollama HTTP client, CLI→server calls |
| pydantic | ≥2.12.0 | Request/response data validation |
| chromadb | ≥1.5.0 | Vector memory database (embedded) |
| sentence-transformers | ≥5.2.0 | Text embeddings (all-MiniLM-L6-v2) |
| pypdf | ≥6.7.0 | PDF text extraction for document ingest |
| mcp | ≥1.26.0 | Model Context Protocol server (Claude Desktop) |

### Development
*(No devDependencies defined — no test framework, linter, or formatter configured)*

---

## Infrastructure

| Service | Provider | Purpose |
|---------|----------|---------|
| Ollama | Self-hosted (localhost:11434) | LLM inference for all AI calls |
| ChromaDB | Embedded (./chroma_memory/) | Persistent vector storage |
| all-MiniLM-L6-v2 | sentence-transformers (CPU) | Embedding generation |

---

## Entry Points / CLI Commands

| Command | Module | Purpose |
|---------|--------|---------|
| `zonny` | `zonny.cli:main` | Interactive CLI chat |
| `zonny-server` | `zonny.server_entry:start` | FastAPI REST server (port 8000) |
| `zonny-mcp` | `zonny.mcp_entry:start` | MCP stdio server for Claude Desktop |
| `python server.py` | `server.py` | Direct FastAPI start |
| `python mcp_server.py` | `mcp_server.py` | Direct MCP server start |

---

## Configuration

| Variable / File | Purpose | Location |
|-----------------|---------|---------|
| `key.json` | API key store (list of valid bearer tokens) | Project root |
| `OLLAMA_URL` | Ollama API endpoint | Hardcoded: `http://localhost:11434/api/generate` |
| `MODEL` | LLM model name | Hardcoded: `nemotron-3-nano:latest` |
| `DB_DIR` | ChromaDB persistence directory | Hardcoded: `./chroma_memory` |
| `agents/manifests/*.yaml` | Agent YAML definitions (system prompt, tools, priority) | `agents/manifests/` |
| `model_capabilities.yaml` | Model capability metadata | Project root |

---

## External Requirements (NOT pip-installed)

| Tool | Install | Required For |
|------|---------|-------------|
| Ollama | https://ollama.com | All LLM inference |
| `nemotron-3-nano:latest` | `ollama pull nemotron-3-nano:latest` | Default model |
| Git | System install | `git.status` tool (optional) |

---

## Outdated Packages

*(No version pinning — all dependencies use `>=` floor constraints. Run `pip list --outdated` inside `venv/` to check current state.)*
